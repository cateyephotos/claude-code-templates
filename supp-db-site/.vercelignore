# Convex backend (deployed separately to convex.dev)
convex/

# supplements-new/ is the staging output of seed.js — supplements/ is the live directory
# Deploying both causes duplicate content (228 near-identical pages)
supplements-new/

# Test/mockup files — not production content
mockups/
compare/mockup-*.html
test-*.html
debug-*.html

# Test files
tests/
playwright.config.js
playwright-report/

# Healing engine (not needed for build or serve)
healing-engine/

# Sprint journal
sprint-journal/

# Dev/config files
.env
.env.local
.env.example
docker-entrypoint.sh
tsconfig.json
package-lock.json

# Documentation
docs/
*.md
# Vercel auto-deploy trigger
